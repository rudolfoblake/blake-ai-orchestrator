# Changelog – Frontend

All relevant changes to the React app are listed here.

## 4.0 – Basic UI and backend integration (2025-11-03)
- Simple React app showing `final_answer` from `POST /infer`
- `REACT_APP_API_URL` config to point at any compatible backend
- Frontend Dockerfile and run via Docker Compose

## 3.x – Initial scaffold
- Base React structure (create-react-app–like)
- Landing page with prompt form

---

## PT-BR

Todas as mudanças relevantes ao aplicativo React estão listadas aqui.

### 4.0 – UI básica e integração com backend (2025-11-03)
- App React simples exibindo `final_answer` consumindo `POST /infer`
- Configuração de `REACT_APP_API_URL` para apontar a qualquer backend compatível
- Dockerfile do frontend e execução via Docker Compose

### 3.x – Scaffold inicial
- Estrutura base do React (similar ao create-react-app)
- Página inicial com formulário de prompt